# 项目亮点总结 (Project Highlights)

> 研究生复试答辩用 — 每个亮点按"是什么 → 为什么重要 → 效果"结构展开
> 6 个亮点分别对应：架构、并发、算法、数据、工程、AI

---

## 亮点 1：分层架构与模块解耦设计

### 是什么

系统采用 **四层架构**：业务层（订单/支付/评价）→ 调度层（派单/评分）→ 学习层（动态反馈）→ 观测层（指标/健康/异常）。层间通过接口通信，ScoringEngine 为可插拔策略接口。

### 为什么重要

在网约车这种业务逻辑快速变化的场景中，**调度策略**（如何匹配司机和乘客）是变化最频繁的部分。如果策略代码散落在业务代码中，每次调整都需要全量回归测试。

通过接口隔离，更换评分策略（如从加权评分换成 ML 模型）只需实现新类 + 改一行注入，现有业务代码零修改。这体现了**开闭原则**（对扩展开放，对修改关闭）。

### 效果

- 新增评分策略：1 个新文件 + 1 行注入改动
- 新增订单状态：枚举中加 1 行 + 流转表加 1 个 entry
- 新增事件类型：recordEvent() 加 1 个调用
- 无循环依赖：所有 Service 依赖方向统一

---

## 亮点 2：高并发派单一致性保障（二阶分布式锁）

### 是什么

多订单并发派单场景下，通过 **Redis Redisson 二阶分布式锁** 保证"一个司机不会被两个订单同时分配、一个订单不会被重复派单"。

```
Phase 1: lock("order:dispatch:{orderId}")    → 防重复派单
Phase 2: tryLock("driver:lock:{driverId}")   → 防重复分配, 500ms 超时
         失败 → fallback 下一位候选
```

### 为什么重要

这是出行调度系统的**核心正确性问题**。在滴滴/Uber 的生产环境中，一个司机被重复分配会导致两个乘客都以为有车来接，至少一人被爽约。单机环境下 synchronized 可以解决，但分布式环境下必须用分布式锁——而且锁的设计必须**无死锁、无活锁、防 ABA**。

### 效果

- **100 订单 × 20 司机并发压测**：零重复分配、零订单丢失、零死锁
- 平均调度延迟：< 150ms（单订单），< 350ms（50 订单批量）
- 数学上无死锁（锁顺序 100% 统一）
- tryLock 机制避免阻塞，高碰撞率下自动降级

---

## 亮点 3：可收敛的动态调度评分引擎

### 是什么

在静态加权评分之上叠加**反馈学习 + 时间衰减**的动态层，解决了"富者愈富"的正反馈循环问题。

```
baseScore = 0.40×距离 + 0.30×评分 + 0.20×空闲 + 0.10×拒单惩罚
finalScore = baseScore + driverModifier

driverModifier 规则:
  接单 +5 分 → 排名上升
  拒单 -10 分 → 排名下降
  10分钟无活动 → modifier 向0回归50%
  上限 +20, 下限 -30
```

### 为什么重要

如果没有反馈机制，排名高的司机会持续拿订单 → 评分更高 → 永远拿订单；排名低的司机永远拿不到订单。这是一个典型的**正反馈循环导致系统不稳定的案例**（类似 PageRank 的阻尼因子、TCP 的拥塞控制——都是用"衰减"来防止震荡）。

### 效果

- 5 轮派单后成功率从 50% 提升至 70%（+20pp）
- 拒单率从 15% 降至 8%（-7pp）
- 评分波动（max|modifier|）从 ±30 收敛到 ±15（-50%）
- 系统在 5 轮内达到稳定状态

---

## 亮点 4：事件溯源式审计追踪与数据一致性

### 是什么

所有订单状态变更以**事件**形式写入 `t_order_event` 表（12 种事件类型），不是操作日志——是轻量级 **Event Sourcing（事件溯源）** 设计。

### 为什么重要

网约车系统的客诉处理有一个经典问题：乘客说"我没上车"，司机说"我等了 10 分钟"——谁在说谎？

事件溯源可以**精确还原订单在每一秒的状态**：创建时间、支付时间、派单给哪位司机、司机何时接单/拒单、何时到达、何时开始行程、何时结束。所有事件带时间戳和操作人 ID，不可篡改。

### 效果

- 一条 SQL 即可回溯任意订单的完整生命周期
- 统计"系统派单 vs 司机手动接单"比例无需联表
- 为未来 CQRS（读写分离）架构打下基础
- 12 种事件类型覆盖业务全流程

---

## 亮点 5：形式化状态机与测试驱动验证

### 是什么

将订单系统的 10 个状态和 15+ 条合法流转路径**形式化为枚举 + 流转表**，不散落 if-else。每个业务方法通过 `guardTransition()` 一行代码防御非法流转。

### 为什么重要

软件工程中有一个经典问题：**"你怎么证明你的系统不会出现非法状态？"**

用 if-else 的状态管理无法回答这个问题——因为你无法穷举所有状态组合。形式化状态机的优势是：
1. **流转规则可见**：一张 `Map<OrderStatus, Set<OrderStatus>>` 说清楚所有合法路径
2. **可穷举测试**：对每个合法路径写一个"应该通过"的测试，对每个非法路径写一个"应该拒绝"的测试
3. **可扩展验证**：新增状态后，测试自动会告诉你有没有破坏已有逻辑

### 效果

- 18 个单元测试覆盖全部合法/非法路径
- 30 秒跑完全部测试
- 新增状态只需修改 1 个文件

---

## 亮点 6：AI 辅助全流程开发实践（Vibe Coding）

### 是什么

本项目从需求分析到架构设计到编码到测试到文档，全程使用 AI（Claude Code + Agent 体系）辅助开发。AI 参与度约 60%，但所有**架构决策、并发设计、状态机形式化均由人工主导**。

### 为什么重要

2025-2026 年，"AI 辅助开发"已成为软件工程领域最热门的话题。研究生复试中，导师极大概率会问："你们用过 AI 写代码吗？怎么用的？"

这个项目提供了一个有说服力的回答：
- 不是"AI 帮我写了所有代码"（这听起来像你没做什么）
- 而是"AI 帮我处理了 60% 的机械性工作，但关键决策——并发安全、状态机设计、评分收敛——都是我做主。而且我建立了一套规则体系来约束 AI 的行为。"

### 效果

- 代码生成效率：2.5× 提升
- 测试生成效率：2× 提升
- 文档生成效率：50× 提升
- 建立了 20 条 AI 行为约束规则
- 核心 Bug（ABA 竞态）由人类发现——验证了"关键逻辑不能全交给 AI"

---

## 一句话总结

> 这是一个**"从业务需求到系统设计到并发优化到 AI 工程实践"的完整项目**，不是一个 CRUD demo。调度层的二阶分布式锁和可收敛评分引擎展示了分布式系统和算法设计能力；状态机形式化和事件溯源展示了软件工程素养；AI 辅助开发实践展示了技术前瞻性。
