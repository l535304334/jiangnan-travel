# 江南出行智慧服务平台 — 项目开发总结报告

> **目标读者**：后续接手本项目的助手 / 开发者 / 实习指导老师
> **撰写日期**：2026-07-03（更新：追加调度系统增强 + 复试表达体系 + 仓库整理）
> **开发者**：赖睿轩（南昌大学软件工程 2307 班）
> **实习单位**：江南出行运输服务有限公司宁都分公司
> **实习周期**：2026.7.6 - 2026.8.6

---

## 一、项目概况

**江南出行**是一个面向江西省的网约车 / 智慧出行平台，采用前后端分离架构，包含 **乘客端、司机端、管理后台** 三个前端 + 一个 Spring Boot 后端，深度集成 **4 大 AI 引擎**（智能客服、出行预测、智能调度、安全风控）与 **高德地图**，并融入 **江西文旅** 元素。

产品定位：**"行遍江西，智享出行"** — 以江西地域文化为灵魂、以智慧出行为核心的一站式出行平台。双引擎战略覆盖市内短途（≤50km 即时出行）与城际长途（>50km 跨城出行）。

项目在 **Claude Code + AI Agent 体系** 协助下，从零完成完整可演示 MVP。经 v1.0-v1.5 六轮迭代，在全栈 CRUD 之上深度增强了**并发调度系统**（二阶分布式锁 + 可收敛评分引擎 + 事件溯源 + 三层可观测性），并构建了完整的**研究生复试表达体系**（面试稿 + 问答库 + 简历版 + 架构图）。

---

## 二、项目结构（当前真实盘点）

```
C:\Users\lai\Desktop\软件工程2307班实习材料\
├── jiangnan-travel/                  # 后端 Spring Boot 3.2.6 项目
│   ├── pom.xml                       # Maven 构建（19 条依赖）
│   └── src/main/java/com/jiangnan/travel/
│       ├── TravelApplication.java    # @SpringBootApplication 启动类
│       ├── common/                   # 通用类（Result/ErrorCode/BusinessException/GlobalExceptionHandler）
│       ├── config/                   # 9 个 @Configuration 配置类
│       ├── security/                 # JwtUtil、JwtAuthFilter、SecurityConfig
│       ├── controller/               # 22 个 Controller → 97 个 API 端点
│       ├── service/ + impl/          # 22 个 Service 接口 + 22 个实现
│       ├── entity/                   # 30 个实体（含 BaseEntity，业务实体 29 个）
│       ├── mapper/                   # 29 个 MyBatis-Plus Mapper
│       ├── dto/                      # 26 个请求 DTO
│       ├── vo/                       # 17 个响应 VO
│       └── websocket/                # 3 个 WebSocket 端点
│   └── src/main/resources/sql/       # 7 个 SQL 脚本（init.sql 建 27 张表）
│
├── jiangnan-travel-web/              # 前端 Vue 3 + Vite 5 项目
│   ├── package.json                  # 8 个运行依赖 + 2 个构建依赖
│   └── src/
│       ├── api/                      # 13 个 API 模块（封装 Axios + 拦截器）
│       ├── components/               # 3 个公共组件（CdnAvatar / AmapView / AiChatFloat）
│       ├── composables/              # 3 个组合式函数（POI搜索 / 反馈 / 验证码）
│       ├── views/                    # 41 个页面视图（乘客 / 司机 / 管理 三端）
│       ├── router/                   # 路由配置 + 三层鉴权守卫
│       ├── stores/                   # Pinia 状态管理
│       └── assets/                   # 全局样式（style.css + transitions.css）
│
├── deploy/                           # Docker 部署（backend/frontend Dockerfile + docker-compose）
├── e2e/                              # Playwright E2E 测试（core-flow.spec.js）
├── docs/                             # 设计文档（产品设计 / 设计系统 / 页面结构 / 路线图 等）
├── .github/workflows/ci.yml          # GitHub Actions CI（Maven + npm 构建）
├── *.docx                            # 实习材料模板文件（不可改动）
│
├── tests/                            # 测试回归网
│   ├── test-suite.mjs                # 全量测试编排器（编排 18 个测试脚本，155 用例）
│   ├── *_test.mjs                    # 18 个分模块测试脚本
│   ├── TEST_GUIDE.md                 # 测试脚本使用说明
│   └── cleanup_test_data.sql         # 测试数据清理脚本
│
├── 实习材料/                          # 实习相关文档
│   └── *.docx, *.doc                 # 实习任务书、鉴定表等（不参与开发）
│
├── AGENTS.md                          # AI 代理总入口
├── ARCHITECTURE.md                    # 架构文档（数据库 / API / 安全 / 缓存 / WebSocket）
├── README.md                          # 项目说明
├── PROJECT_HIGHLIGHTS.md              # 项目亮点总结（复试用）
├── CHANGELOG.md                       # 更新日志
├── docs/                              # 设计文档 + 面试材料
│   ├── 江南出行调度系统_系统设计文档_v1.5.md
│   ├── 江南出行_简历面试表达层_v1.5.md
│   ├── ai-development-log.md
│   ├── system-design.md → 已合并至 v1.5
│   ├── interview/                     # 复试面试稿
│   ├── resume/                        # 简历表达
│   └── architecture/                  # 架构图
└── 项目开发总结报告.md                 # 本文件
```

---

## 三、技术栈

| 层级 | 技术 | 版本 |
|---|---|---|
| 后端框架 | Spring Boot | 3.2.6 |
| JDK | Java | 17 |
| 安全 | Spring Security + JWT (jjwt) | 0.12.5 |
| ORM | MyBatis-Plus | 3.5.7 |
| 数据库 | MySQL | 8.0.42 |
| 缓存 / 分布式锁 | Redis + Redisson | 3.32.0 |
| 实时通信 | WebSocket（Spring 原生） | — |
| AI | DeepSeek API（openai-java） | 0.18.0 |
| 接口文档 | Knife4j | 4.5.0 |
| 监控 | Spring Boot Actuator + Micrometer Prometheus | — |
| 前端框架 | Vue 3 + Composition API | 3.4 |
| 构建 | Vite | 5 |
| UI 库 | Element Plus | 2.7 |
| 状态管理 | Pinia | 2.1.x |
| HTTP 客户端 | Axios | — |
| 地图 | 高德 JS API | 2.0 |

---

## 四、4 周开发历程

### Week 1 — 基础搭建 + 用户模块 + AI 客服
- Maven 项目骨架 + MyBatis-Plus + Spring Security + JWT 完整配置
- 27 张表 DDL + 种子数据 SQL
- 用户注册 / 登录（密码 + 验证码双模式）
- AI 智能客服（DeepSeek，注入江西文旅知识库，含降级方案）
- AI 出行预测引擎（按时段统计 + 新用户冷启动）
- 收藏地址 CRUD
- **典型 Prompt**："根据 t_user 表 DDL 生成 MyBatis-Plus 实体类，使用 Lombok"

### Week 2 — 订单核心 + AI 调度
- 阶梯计价引擎（0-3km / 3-50km / 50-200km / 200+km 四档）
- AI 智能调度引擎（距离 0.5 + 评分 0.3 + 空闲 0.2 多因素加权）
- 动态调价引擎（供需比算法，上限 1.5x） — 已移除，未实际启用
- Redisson 分布式锁（防止重复接单 / 支付幂等）
- 订单全生命周期（创建→接单→到达→开始→完成→支付→评价）
- 优惠券系统（领取 / 使用 / 新用户发券）
- R2 取消频率、R4 深夜跨城、R7 疲劳驾驶 三条风控规则

### Week 3 — 前端三端 + WebSocket + 高德地图 + 实时追踪
- Vue 3 项目搭建（Vite 5，三端路由体系）
- 三合一登录页（乘客 / 司机 / 管理员切换，密码 + 验证码双模式）
- 三端全部页面组件
- WebSocket 实时推送（司机位置 `/ws/driver-location` + 订单状态 `/ws/order/{id}` + 通知 `/ws/notification`）
- 高德地图组件 AmapView（江西地标坐标库 + Haversine 距离计算）
- AI 客服悬浮窗（DeepSeek 对话）
- 行程追踪页文化短句轮播

### Week 4 — 集成测试 + 优化 + 文档 + Git 部署
- Axios 拦截器修复（业务错误统一 reject + ElMessage 提示）
- IP 限流（60 次/分钟/ip，Redis 滑动窗口）
- Redis 缓存（多级 TTL：车型 / 地标 / 寄语 / 热点）
- SQL 复合索引优化（含地理查询、高频查询）
- 管理后台真实接口联动（从静态数据升级为真实 API）
- Git 本地仓库 + post-commit 自动 CHANGELOG + GitHub 推送

### 接手优化阶段（2026-06-24 之后）
- 认证体系标准化（401/403 错误码统一、密码校验逻辑调整）
- 订单核心问题修复（优惠券 ID 语义、司机评分同步、取消状态恢复、重复支付拦截）
- 安全加固（支付回调接口、JWT 密钥长度、SecurityConfig 权限顺序、IDOR 越权防护）
- 性能优化（订单统计索引、Vite 分包 / ECharts 懒加载、大屏 Redis 缓存、慢查询 AOP 监控）
- 测试基建（18 个测试脚本 + 全量编排器 + 数据清理脚本 + Playwright E2E）
- 监控集成（Actuator + Prometheus 端点）
- CI 流水线（GitHub Actions 后端 Maven + 前端 npm 构建）
- 图片 CDN 抽象（CdnAvatar 组件统一头像来源）

---

## 五、关键技术决策

| 决策 | 原因 |
|---|---|
| 包名 `com.jiangnan.travel`（非 `smarttravel`） | 开发中简化，避免过长包名 |
| 密码登录为主要认证方式 | 验证码依赖运营商，沙箱环境不可用 |
| 高德地图配置 `_AMapSecurityConfig` | 高德 JS API 2.0 要求安全密钥 |
| 地标坐标本地化（预设点） | 避免高德地理编码 API 额外调用 |
| 风控实现 R2+R4+R7 | 按 MVP 可降级计划，其余为生产级扩展 |
| Post-commit Hook 自动 CHANGELOG | bash 脚本，每次 `git commit` 后触发 |
| 支付幂等键 `pay:idempotent:orderId:userId` | Redisson 分布式锁防止重复支付 |
| 大屏 Redis 缓存 TTL 2 分钟 | 降低管理后台 dashboard 统计压力 |
| 慢查询阈值 500ms | AOP 切面监控 Service 方法执行耗时 |

---

## 六、运行方式

### 前置条件
- MySQL 8.0（库 `smart_travel`，已初始化）
- Redis（端口 6379）
- JDK 17 / Maven 3.9+
- Node.js 18+（推荐 22.x）

### 1. 初始化数据库
```bash
mysql -u root -p < jiangnan-travel/src/main/resources/sql/init.sql
mysql -u root -p smart_travel < jiangnan-travel/src/main/resources/sql/indexes.sql
mysql -u root -p smart_travel < jiangnan-travel/src/main/resources/sql/test_accounts.sql
```

### 2. 配置应用
修改 `jiangnan-travel/src/main/resources/application.yml`：
- `spring.datasource.username/password` — MySQL 连接信息
- `deepseek.api-key` — DeepSeek API Key
- `amap.web-api-key` / `amap.js-api-key` — 高德地图 Key

### 3. 启动后端
```bash
cd jiangnan-travel
mvn spring-boot:run
# → http://localhost:8080
# → API 文档: http://localhost:8080/doc.html （Knife4j）
# → 监控端点: http://localhost:8080/actuator/health|prometheus
```

### 4. 启动前端
```bash
cd jiangnan-travel-web
npm install
npm run dev
# → http://localhost:5173
```

### 5. 运行测试
```bash
# 后端单元测试
cd jiangnan-travel && mvn test

# 全量 API 测试套件（需后端 + 前端已启动）
node tests/test-suite.mjs

# 跳过安全测试分组
node tests/test-suite.mjs --skip-security

# E2E 测试
cd e2e && npx playwright test
```

---

## 七、测试账号

| 角色 | 账号 | 密码 | 登录入口 |
|---|---|---|---|
| 乘客 | 13900001111 | 123456 | `/login` |
| 乘客 | 13920000001 | 123456 | `/login` |
| 司机 | 13810000001 | 123456 | `/login`（司机标签） |
| 管理员 | admin | 123456 | `/login`（管理员标签） |

> 密码均经 BCrypt 加密存储。`init.sql` 种子数据与 `test_accounts.sql` 测试账号统一使用同一 BCrypt 哈希，明文为 `123456`。

---

## 八、API 接口概览（97 个端点）

> 完整接口清单见 [ARCHITECTURE.md](ARCHITECTURE.md) 第五章。

| 路径前缀 | 端点数 | Controller |
|---|---|---|
| `/api/admin` + `/api/admin/ai` | 25 | AdminManageController(23) + AdminController(1) + AiInsightController(1) |
| `/api/order` | 9 | OrderController |
| `/api/driver/order` | 8 | DriverOrderController |
| `/api/user` + `/api/user/address` | 10 | UserController(7) + UserAddressController(3) |
| `/api/driver` | 7 | DriverController(5) + DriverEarningController(2) |
| `/api/ai` | 5 | AiChatController(4) + AiPredictionController(1) |
| `/api/invoice` | 5 | InvoiceController |
| `/api/vip` | 4 | VipController |
| `/api/notification` | 4 | NotificationController |
| `/api/payment` | 4 | PaymentController |
| `/api/campaign` | 3 | CampaignController |
| `/api/coupon` | 3 | CouponController |
| `/api/bus-line` | 3 | BusLineController |
| `/api`（根，文旅/AI 数据） | 3 | AIDataController |
| `/api/landmark` | 2 | CityLandmarkController |
| `/api/route` | 1 | RouteController |
| `/api/safety` | 1 | SafetyController |
| **合计** | **97** | 22 个 Controller |

---

## 九、数据库（27 张表）

| 模块 | 表 |
|---|---|
| 用户 / 权限 | `t_user`, `t_driver`, `t_admin` |
| 订单核心 | `t_order`, `t_order_track`, `t_payment`, `t_invoice` |
| 营销 | `t_coupon`, `t_user_coupon`, `t_campaign`, `t_campaign_coupon` |
| 评价 / 地址 | `t_review`, `t_user_address` |
| 车型 | `t_car_type` |
| 文旅 | `t_city_landmark`, `t_city_quote` |
| AI | `t_ai_chat_log` |
| 客运班线 | `t_bus_line`, `t_bus_schedule`, `t_schedule_route`, `t_schedule_order` |
| VIP | `t_vip_level`, `t_user_vip` |
| 消息 | `t_notification` |
| 风控 / 推送 | `t_demand_hotspot`, `t_risk_alert`, `t_user_risk_profile`, `t_push_log` |

> 完整表结构见 [ARCHITECTURE.md](ARCHITECTURE.md) 第四章。

---

## 十、AI 开发环境能力

### 10.1 模型与工具
- **主要工具**：Claude Code CLI（支持 Tool Calling / 多文件编辑 / 子 Agent / MCP 集成）
- **辅助工具**：Trae IDE（早期阶段）/ DeepSeek API（业务层 AI 客服）
- **覆盖范围**：需求分析 → 设计 → 编码 → 测试 → 审查 → 部署 → 文档全流程
- **约束体系**：CLAUDE.md 20 Rules（全局）+ `.claude/CLAUDE.md`（项目级）

### 10.2 Skills（33 个绑定）
详见 CLAUDE.md Section 三（Skill 自动绑定表）。核心 Skill：ponytail（简洁编码）、verification-loop（改动验证）、code-reviewer（自动审查）、continuous-learning-v2（长期知识积累）。

### 10.3 MCP（5 个）
| MCP | 用途 |
|-----|------|
| Filesystem | 文件读写 / 搜索 / 编辑 |
| Git | commit/diff/log/branch/worktree |
| MySQL | 只读查询 / schema 检查 |
| Playwright | 浏览器自动化 / E2E |
| Sequential Thinking | 复杂分步推理 |

### 10.4 环境健康度
- 健康评分：95 / 100
- Claude Code：v2.1.199（npm global @ Roaming/npm）
- 运行时：Node.js v22.19.0 / npm 11.18.0 / Java 17 / MySQL 8.0.42
- 等级：高级（Advanced）— 完整 Skill 生态 + Agent 体系 + MCP 覆盖

---

## 十一、测试与优化成果

项目经过多轮全功能测试与优化，当前达到稳定可演示状态。详细测试矩阵、问题修复清单与优化项见 [.trae/documents/full-project-testing-optimization-report.md](.trae/documents/full-project-testing-optimization-report.md)。

| 维度 | 结果 |
|---|---|
| 测试用例总数 | 155 |
| 通过 / 失败 | 155 / 0 |
| 通过率 | 100% |
| 测试脚本 | 18 个（核心链路 5 + 业务专项 6 + 安全边界 4 + 并发风控 3） |
| E2E 测试 | 3 个核心流程通过 |
| 前端构建 | 通过 |
| 后端编译 | 通过 |

**关键修复与优化：**
- 认证体系：401/403 错误码统一、密码校验逻辑调整
- 订单核心：优惠券 ID 语义、司机评分同步、取消状态恢复、重复支付拦截
- 安全加固：支付回调接口、JWT 密钥长度、SecurityConfig 权限顺序、IDOR 越权防护、SQL/XSS 输入过滤
- 性能优化：订单统计索引、Vite 分包 / ECharts 懒加载、大屏 Redis 缓存（TTL 2min）、慢查询 AOP 监控（500ms 阈值）
- 测试基建：全量编排器、数据清理脚本、Playwright E2E
- 监控：Actuator + Prometheus 端点
- CI：GitHub Actions（Maven + npm 构建）
- 图片 CDN：CdnAvatar 组件统一头像来源

---

## 十二、当前完成度评估

| 模块 | 完成度 | 说明 |
|---|---|---|
| 乘客端 | 高 | 登录、下单、估价、订单、追踪、地址、优惠券、客服均已具备演示能力 |
| 司机端 | 中高 | 登录、上线、接单、状态流转、收入页已有；真实位置推送可继续加强 |
| 管理后台 | 中高 | 已从静态数据升级为真实接口联动；大屏 / 用户 / 司机 / 订单 / 告警 / 车型 / 活动 / VIP / 班线管理 |
| 后端核心 | 高 | 用户、订单、调度、计价、优惠券、支付、发票、后台接口、风控均已实现 |
| AI 能力 | 中 | 客服、推荐、调价、调度、风控概念齐全；部分偏规则算法，真实智能化深度可继续提高 |
| 地图与实时 | 中 | 高德地图组件 + WebSocket 端点齐全；生产级心跳、断线重连、轨迹持久化不足 |
| 测试与交付 | 高 | 155 用例 100% 通过 + E2E + CI 流水线 |

---

## 十三、商业与落地可行性

| 维度 | 判断 | 原因 |
|---|---|---|
| 学术 / 实习交付 | 高 | 技术栈完整、功能闭环清晰、文档充分、测试覆盖高，适合展示软件工程实践过程 |
| 企业原型验证 | 中高 | 能支撑业务流程演示，真实运营前需补齐合规、支付、地图、短信、运维、安全 |
| 真实商业上线 | 中低 | 网约车业务强监管，必须具备运营许可证、司机 / 车辆资质、资金结算、隐私合规、风控审计 |
| 本地化差异化 | 中高 | 江西文旅融入是亮点，还需更具体的城市 / 景区 / 线路运营场景拉开差异 |

**主要风险：**
1. 合规风险：真实网约车业务需先满足平台、车辆、司机资质要求
2. 供需冷启动：无司机供给时乘客体验断裂，需模拟派单或种子司机方案
3. 地图依赖：高德 Key、服务开通、域名白名单、配额影响演示稳定性
4. 支付与发票：当前为模拟支付，电子发票为表结构占位，不能支撑真实交易
5. AI 宣称边界：5 个 AI 引擎中部分能力实为规则算法或静态数据，文档应明确 MVP 口径

---

## 十四、调度系统增强（v1.0-v1.5，实习后持续迭代）

实习结束后，继续对调度系统进行了六轮工程增强，从"功能可运行"升级为"可展示工程深度"：

| 版本 | 核心增量 | 关键设计 |
|------|---------|---------|
| v1.0 | 形式化订单状态机 | OrderStatus 枚举 + ALLOWED_TRANSITIONS Map |
| v1.1 | 业务闭环 | DriverAssignment + Billing + Review |
| v1.2 | 并发调度 | 二阶分布式锁 + ScoringEngine + DriverStatus |
| v1.3 | 动态学习 | DynamicScoringEngine（反馈±衰减）+ 50订单压测 |
| v1.4 | 收敛验证 | AnomalyDetection + HealthScore + 100订单极端压测 |
| v1.5 | 工程整理 | 系统设计文档 + 面试表达体系 + 仓库整理 |

**核心技术成果**：
- **并发控制**：二阶分布式锁（order lock → driver tryLock），100订单零重复分配
- **评分引擎**：加权评分 + 动态反馈学习 + 10min半衰期衰减，5轮收敛
- **事件溯源**：order_event 表覆盖 12 种事件类型
- **可观测性**：Metrics / Health / Anomaly 三层体系
- **AI 实践**：人类主导 40% + AI 执行 60%，20 条规则约束

> 详细设计见 `docs/江南出行调度系统_系统设计文档_v1.5.md`

---

## 十五、已知问题与后续方向

| 项目 | 状态 | 说明 |
|---|:--:|---|
| 验证码 | ⚪ | 沙箱环境不可见，终端打印；真实环境需接入短信 SDK |
| 高德地图 | ⚠️ | Key 已配置，需在 Amap 控制台开启 JS API 服务 |
| 风控 R1/R3/R5/R6 | ⚪ | 按 MVP 可降级 |
| 电子发票 | ⚪ | 已有表结构与接口，真实开票需对接税务系统 |
| 班线系统 | ⚪ | 已有表结构与基础接口，运营场景可继续丰富 |
| WebSocket 生产化 | ⚪ | 需添加心跳 + 断线重连 + 集群方案 |
| 微信分享 | ⚪ | `/order/{id}/share` 有接口，缺微信 SDK 集成 |
| 图片 CDN | ⚪ | 已抽象 CdnAvatar 组件，真实 CDN 部署可选 |

---

## 十五、Git 仓库

| 项目 | 内容 |
|---|---|
| 远程仓库 | https://github.com/l535304334/jiangnan-travel |
| 本地路径 | `C:\Users\lai\Desktop\软件工程2307班实习材料` |
| 提交方式 | `git add -A && git commit -m "..." && git push` |
| 自动日志 | 每次 commit 后 `CHANGELOG.md` 自动追加一条记录 |
| Git 用户 | 赖睿轩 / 535304334@qq.com |
| 忽略规则 | `target/` `node_modules/` `.idea/` `dist/` `*.log` `*.class` `.env` 等 |

---

## 十六、设计文档对照

设计文档统一存放于 `docs/` 目录：

| 文档 | 用途 |
|------|------|
| `江南出行调度系统_系统设计文档_v1.5.md` | 完整系统设计（架构/并发/评分/复杂度） |
| `江南出行_简历面试表达层_v1.5.md` | 简历描述 + 30s/2min 面试脚本 |
| `ai-development-log.md` | AI 辅助开发全流程记录 |
| `system-design.md` → 已合并至 v1.5 | 系统设计说明（英文版，内容已合并） |
| `interview/interview_script.md` | 3min+5min 复试背诵稿 |
| `interview/interview_qna.md` | 8 个面试问答 + 速查卡 |
| `interview/project_highlights_interview.md` | 8 个技术亮点深度展开 |
| `resume/project_description.md` | 150字/100字 简历版本 |
| `architecture/interview_architecture.md` | 白板架构图 + 讲解提示 |
| `ARCHITECTURE.md` (根目录) | 完整数据库/API/安全/缓存/WebSocket 架构 |

---

*本文档基于 2026-06-30 项目状态初稿，于 2026-07-03 更新以反映 v1.0-v1.5 调度系统增强、复试表达体系构建和仓库整理。后续开发者可据此快速了解项目全貌。*
