# EXECUTION_MODE.md — 企业级项目执行模式

> 项目：江南出行（Jiangnan Travel）  
> 生效日期：2026-06-24  
> 约束力：**本文件定义项目执行模式下的全部行为规范，所有开发任务默认在此模式下执行。**

---

## 一、执行模式概述

### 1.1 什么是执行模式

执行模式（Execution Mode）是项目的**默认开发状态**。在此模式下：

- 收到开发指令后，AI **自动执行**完整开发流程，无需用户重复说明
- 每个 Task 完成后，AI **自动输出开发报告**，等待用户确认后再继续
- 开发过程严格遵循全部设计文档，**不得擅自变更**任何已确认的设计方案

### 1.2 何时进入执行模式

| 触发条件 | 行为 |
|---|---|
| 用户发送"继续开发"等开发指令 | 自动进入执行模式 |
| 用户发送"开发 Task-NNN"等指令 | 自动进入执行模式 |
| 用户发送"退出执行模式" | 退出执行模式，回到常规对话模式 |
| 新会话开始时 | 默认进入执行模式 |

### 1.3 执行模式流程图

```
收到开发指令
     │
     ▼
┌─────────────────────────────────────────────────────┐
│                  执行模式启动                          │
├─────────────────────────────────────────────────────┤
│  ① 加载所有项目规范文档                                │
│  ② 扫描项目当前状态                                   │
│  ③ 定位当前应开发的 Task                              │
│  ④ 分析影响范围与依赖关系                              │
│  ⑤ 检查是否存在设计冲突                                │
└─────────────────────┬───────────────────────────────┘
                      │  无冲突
                      ▼
┌─────────────────────────────────────────────────────┐
│                  执行单个 Task                         │
├─────────────────────────────────────────────────────┤
│  ① 数据库设计（如需）                                  │
│  ② API 设计（如需）                                    │
│  ③ 测试先行（TDD）                                     │
│  ④ 编码实现                                           │
│  ⑤ 编译检查 + 静态检查                                  │
│  ⑥ 自动测试                                           │
│  ⑦ Code Review                                        │
│  ⑧ 重构（如需）                                        │
│  ⑨ 文档同步                                           │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│                  开发报告输出                           │
├─────────────────────────────────────────────────────┤
│  ✓ 本次完成内容                                       │
│  ✓ 修改文件 / 新增文件 / 删除文件                       │
│  ✓ 数据库变化 / API 变化 / 页面变化                     │
│  ✓ 风险分析                                           │
│  ✓ 建议下一步开发的 Task                               │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
               等待用户确认
                      │
               ┌──────┴──────┐
               │             │
            确认继续        退回修改
               │             │
               ▼             ▼
         下一项 Task     修复问题后重试
```

---

## 二、启动流程（详细版）

每次收到开发需求时，自动执行以下 5 步启动流程：

### 步骤 1：加载项目规范

默认读取并同步以下文件（按优先级顺序）：

| 优先级 | 文件 | 用途 |
|---|---|---|
| **P0** | `AGENTS.md` | AI 总入口，获取项目概览和默认行为 |
| **P0** | `EXECUTION_MODE.md` | 本文件，获取执行流程和纪律 |
| **P1** | `PROJECT_RULES.md` | 编码规范 |
| **P1** | `ARCHITECTURE.md` | 架构文档（数据库、API、前端架构） |
| **P1** | `AI_WORKFLOW.md` | Skill + MCP 工作流 |
| **P2** | `PRODUCT_DESIGN.md` | 产品设计文档 |
| **P2** | `DESIGN_SYSTEM.md` | 设计系统规范 |
| **P2** | `UI_UX_SPEC.md` | UI/UX 规范 |
| **P2** | `PAGE_STRUCTURE.md` | 页面结构文档 |
| **P2** | `FEATURE_ROADMAP.md` | 功能路线图 |
| **P2** | `TASK_ROADMAP.md` | 开发任务路线图 |
| **P2** | `DEVELOPMENT_PLAN.md` | 开发计划 |
| **P2** | `项目开发总结报告.md` | 项目开发总结报告（含审查、能力、历程） |
| **P3** | `README.md` | 项目说明 |

> **冲突处理**：如果发现上述文件之间存在冲突，必须立即停止，分析原因并给出至少两种解决方案，等待用户确认。不得自行决定修改。

### 步骤 2：扫描项目当前状态

- 查看后端代码状态（`jiangnan-travel/src/`）
- 查看前端代码状态（`jiangnan-travel-web/src/`）
- 检查数据库表结构（通过 MySQL MCP）
- 查看 Git 提交历史
- 记录当前已完成的 Task 和未完成的 Task

### 步骤 3：定位当前 Task

- 对照 `TASK_ROADMAP.md` 和 `DEVELOPMENT_PLAN.md`
- 找到优先级最高且尚未完成的 Task
- 如果存在依赖未完成的 Task，先处理依赖 Task

### 步骤 4：分析影响范围

针对选定的 Task，分析：

- **后端模块**：涉及哪些 Controller / Service / Mapper / Entity
- **前端页面**：涉及哪些 View / Component / Store / API 模块
- **数据库**：是否需要新增表或修改表结构
- **API**：是否需要新增或修改端点
- **依赖关系**：是否有其他 Task 依赖本 Task 的输出

### 步骤 5：冲突检查

- 检查是否与现有代码架构冲突
- 检查是否与设计文档描述冲突
- 检查是否与已有 API 风格冲突
- 如存在冲突 → 按"冲突处理"规则执行
- 如无冲突 → 开始开发

---

## 三、Task 推进规则

### 3.1 单 Task 执行流程

```
┌─────────────────────────────────────────────────┐
│              单 Task 执行流程                      │
├─────────────────────────────────────────────────┤
│                                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ ① 数据库   │→│ ② API    │→│ ③ 测试先行 │       │
│  │  设计(如需) │ │  设计(如需)│ │  (TDD)   │       │
│  └──────────┘  └──────────┘  └────┬─────┘       │
│                                    │               │
│                                    ▼               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ ⑥ Code   │←│ ⑤ 自动测试 │←│ ④ 编码实现 │       │
│  │  Review  │ │  + 编译检查 │ │         │       │
│  └────┬─────┘  └──────────┘  └──────────┘       │
│       │                                           │
│       ▼                                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ ⑦ 重构   │→│ ⑧ 文档同步 │→│ ⑨ 开发报告 │       │
│  │  (如需)  │ │         │ │  输出   │       │
│  └──────────┘  └──────────┘  └──────────┘       │
│                                                   │
└─────────────────────────────────────────────────┘
```

### 3.2 执行原则

1. **一次一个 Task**：每次只完成一个 Task，不得跨多个 Task
2. **不跳步骤**：每个 Task 必须完整执行所有适用的子步骤
3. **完成后即 Review**：每个 Task 完成后立即进入 Code Review
4. **Review 不通过 -> 退回修改**：严重问题退回步骤 ④ 修改
5. **测试必须通过**：测试不通过不得进入下一步

### 3.3 Task 间依赖处理

```
Task A ──依赖──→ Task B ──依赖──→ Task C
                     │
                     └── 并行任务：Task D（无依赖）
```

- **串行依赖**：必须按依赖顺序执行
- **并行任务**：无依赖关系的 Task 可并行执行
- **阻塞等待**：如果 Task 被阻塞（如等待接口联调），先切换到可执行的任务
- **依赖变更**：如果 Task 完成后发现依赖关系变化，更新 `TASK_ROADMAP.md`

### 3.4 执行顺序优先级

```
1. Phase 顺序执行（Phase 1 → Phase 2 → ...）
2. 同一 Phase 内按 Task 编号顺序执行（Task-001 → Task-002 → ...）
3. 需要先处理的依赖 Task 优先于当前 Phase
```

---

## 四、文档维护规则

### 4.1 每次 Task 完成后必须更新的文档

| 文档 | 更新条件 | 更新内容 |
|---|---|---|
| `TASK_ROADMAP.md` | 每次 Task 完成 | 标记 Task 状态为已完成，更新进度 |
| `DEVELOPMENT_PLAN.md` | 每次 Task 完成 | 更新进度百分比，记录实际工时 |
| `ARCHITECTURE.md` | 数据库/API/页面有变化 | 同步变更到对应章节 |
| `PROJECT_RULES.md` | 发现新规范需要记录 | 补充新的编码规则 |
| `AI_WORKFLOW.md` | 工作流有调整 | 同步调整后的流程 |
| `项目开发总结报告.md` | 新增功能模块 | 更新总结报告中的能力与历程章节 |
| `CHANGELOG.md` | 每次提交前 | 记录变更摘要 |
| 相关设计文档 | 设计有变化 | 同步设计变更 |

### 4.2 文档更新规范

- **增量更新**：只在现有文档基础上修改受影响的章节，不重写全文
- **一致性**：确保更新后的文档与代码实际状态一致
- **可追溯**：记录变更原因和变更时间
- **引用关系**：更新文档时同步检查其他文档中的引用是否有效

### 4.3 文档冲突处理

当发现文档之间存在冲突时：

```
① 立即停止开发
② 分析冲突原因（哪个文件正确？哪个文件过时？）
③ 给出至少两种解决方案
④ 分析每种方案的优缺点
⑤ 等待用户确认
⑥ 未经确认不得继续开发
```

---

## 五、开发纪律

### 5.1 强制性纪律

- ✅ **严格遵循设计文档**：不得擅自改变产品设计、系统架构、数据库设计及开发规划
- ✅ **三端统一**：用户端、司机端、管理员端功能、交互和视觉风格统一
- ✅ **遵循 DESIGN_SYSTEM.md 与 UI_UX_SPEC.md**：所有前端开发必须遵循设计规范
- ✅ **优先复用**：优先复用已有组件、工具函数、业务逻辑，不重复造轮子
- ✅ **代码规范统一**：模块边界清晰、可维护、可扩展
- ✅ **数据一致性**：数据库设计、接口设计及状态流转保持一致
- ✅ **单 Task 专注**：每次只完成一个 Task，不跨多个 Task
- ✅ **完整交付**：每个 Task 完成后必须完成测试、Review、重构及文档同步
- ✅ **方案固化**：未经确认不得修改已经确定的设计方案

### 5.2 禁止行为

- ❌ 跳过需求分析和 Spec 直接编码
- ❌ 需求不明确时猜测实现
- ❌ 一次性生成整个项目或整个模块
- ❌ 一次完成多个 Task 不经 Review
- ❌ 生成 TODO / 占位符 / FIXME 代码
- ❌ 生成未使用的代码或注释掉的代码
- ❌ 跳过测试直接提交
- ❌ 未经 Review 提交 Git
- ❌ 直接修改数据库数据（需经用户确认）
- ❌ 修改已确认的设计方案（需经用户确认）

### 5.3 冲突发现时的处理

如果发现设计文档、数据库、代码、接口、页面或业务逻辑之间存在任何冲突：

```
┌─────────────────────────────────────────────────────┐
│              冲突处理流程                              │
├─────────────────────────────────────────────────────┤
│  ① 立即停止开发                                      │
│  ② 分析问题原因                                      │
│  ③ 列出冲突的具体位置和内容                            │
│  ④ 给出至少两种解决方案                                │
│  ⑤ 分析每种方案的优缺点                                │
│  ⑥ 等待用户确认                                      │
│  ⑦ 未经确认不得继续开发                                │
└─────────────────────────────────────────────────────┘
```

### 5.4 长期行为准则

- **默认执行模式**：除非用户明确要求退出，否则永久保持执行模式
- **全自动响应**：用户只需输入"继续开发""开始下一项任务"等简洁指令，AI 自动完成全部流程
- **持续优化建议**：如果发现现有规则存在冗余、冲突或可优化之处，可提出建议
- **未经确认不修改**：即使发现优化点，也需经用户确认后方可修改规范文件

---

## 六、停止条件

### 6.1 正常停止

| 条件 | 说明 |
|---|---|
| 单个 Task 完成并输出开发报告 | 等待用户确认后继续 |
| 全部 Task 完成 | 整个项目开发完毕 |

### 6.2 异常停止

| 条件 | 行为 |
|---|---|
| 发现设计冲突 | 停止 → 分析 → 报告 → 等待确认 |
| 构建失败 3 次 | 停止 → 报告错误原因 |
| 用户要求停止 | 立即停止当前操作 |
| MCP 连接异常 | 降级使用直接工具，如无法继续则报告 |
| 需求不明确 | 停止 → 向用户提问澄清 |

### 6.3 停止后的恢复

```
停止状态
    │
    ▼
┌──────────────────────┐
│  记录当前进度          │
│  (已完成的步骤/文件)   │
└──────────────────────┘
    │
    ▼
┌──────────────────────┐
│  等待用户指令          │
│  继续 / 放弃 / 重做   │
└──────────────────────┘
    │
    ▼
继续 → 从停止处恢复执行
放弃 → 更新文档后关闭 Task
重做 → 重新执行当前 Task
```

---

## 七、汇报规范

### 7.1 开发报告格式

每个 Task 完成后，自动输出以下格式的开发报告：

```markdown
## 开发报告 — Task-NNN: Task 名称

### 本次完成内容
[简要描述本次 Task 完成了什么功能]

### 文件变更
| 类型 | 文件 | 说明 |
|---|---|---|
| 修改 | `path/to/file.java` | 修改了什么内容 |
| 新增 | `path/to/new/file.vue` | 新增了什么内容 |
| 删除 | `path/to/deleted/file.java` | 删除了什么 |

### 数据库变化
| 类型 | 对象 | 说明 |
|---|---|---|
| 新增表 | `t_xxx` | 新增表格 |
| 修改字段 | `t_xxx.yyy` | 修改了字段 |

### API 变化
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/xxx/create` | 新增接口 |
| GET | `/api/xxx/list` | 修改接口 |

### 页面变化
| 路由 | 页面 | 说明 |
|---|---|---|
| `/xxx` | `XxxView.vue` | 新增页面 |

### 风险分析
- [风险描述] → [影响] → [缓解措施]

### 建议下一步开发
推荐下一个开发的 Task：**Task-NNN: Task 名称**
- 原因：[为什么推荐这个 Task]
- 依赖：[该 Task 的依赖条件]
```

### 7.2 报告输出时机

| 时机 | 报告类型 |
|---|---|
| 每个 Task 完成后 | 完整开发报告 |
| 发现设计冲突时 | 冲突分析报告（含解决方案） |
| 构建/测试失败时 | 错误分析报告（含根因和修复方案） |
| 用户主动询问进度时 | 进度状态报告 |

### 7.3 进度状态报告格式

当用户询问开发进度时，输出：

```markdown
## 项目开发进度

**总体进度**：[完成百分比]%（已完成 N 个 / 共 M 个 Task）

### Phase 执行情况
| Phase | 完成 | 未完成 | 进度 |
|---|---|---|---|
| Phase 1: 技术债偿还 | N/M | [列表] | XX% |
| Phase 2: 核心功能补全 | N/M | [列表] | XX% |
| ... | ... | ... | ... |

### 当前执行状态
- **状态**：执行中 / 等待确认 / 已停止
- **当前 Task**：Task-NNN
- **当前步骤**：[步骤名称]
- **阻塞原因**：无 / [原因描述]

### 风险状态
| 风险 | 状态 | 说明 |
|---|---|---|
| [风险1] | ✅ 已解决 | ... |
| [风险2] | ⚠️ 存在 | ... |
```

---

## 八、Skills 与 MCP 调用原则

### 8.1 选择策略

- 根据任务类型**自动选择**最合适的 Skill
- **同一阶段只使用一个 Skill**，避免功能重复调用
- 优先使用**已安装的 Skills 与 MCP**
- 不要为了完成任务而安装新的 Skill 或 MCP

### 8.2 Skill 映射速查

| 阶段 | 首选 Skill | 替代方案 |
|---|---|---|
| 需求分析 | `brainstorming` | 直接提问 |
| Spec 编写 | `spec-driven-development` | — |
| Task 拆分 | `planning-and-task-breakdown` | TodoWrite 直接拆分 |
| 数据库设计 | `database-design` | MySQL MCP 手动设计 |
| API 设计 | `rest-api-design` | 直接按照规范设计 |
| 后端开发 | `java-architect` | 直接编码 |
| 前端开发 | `web-dev` | 直接编码 |
| 测试 | `test-driven-development` | 直接写测试 |
| Code Review | `code-review-and-quality` | Git MCP 手动审查 |
| 重构 | `code-refactoring` | 手动重构 |
| 安全 | `security-and-hardening` | 手动检查 |
| 性能 | `performance-optimization` | 手动优化 |
| 调试 | `debugging-and-error-recovery` | 手动调试 |
| 对抗审查 | `doubt-driven-development` | 手动审查 |
| 文档 | `documentation-and-adrs` | 直接更新 |

> 详细的 Skill 和 MCP 调用矩阵请参考 `AI_WORKFLOW.md`。

### 8.3 MCP 调用原则

| MCP | 用途 | 权限 |
|---|---|---|
| **MySQL** | 数据库查询、表结构读取、数据验证 | 只读 |
| **Git** | 状态查询、差异对比、提交 | 有写权限 |
| **Playwright** | 浏览器自动化、E2E 测试 | 浏览器操作 |
| **Filesystem** | 文件读写、目录遍历 | 需用户确认 |
| **Sequential Thinking** | 复杂推理、架构决策 | 无写权限 |

详细 MCP 工具使用方式请参考 `AI_WORKFLOW.md`。

---

## 九、附录

### 9.1 执行模式速查卡

```
开发指令 → 加载规范 → 扫描项目 → 定位 Task → 分析影响 → 冲突检查
    → [无冲突] 执行 Task → 编译检查 → 静态检查 → 自动测试
    → Code Review → 重构 → 文档更新 → 输出报告 → 等待确认
```

### 9.2 与本文件配合使用的文档

| 文档 | 关系 |
|---|---|
| `AGENTS.md` | AI 总入口，本文件是其执行模式的具体实现 |
| `AI_WORKFLOW.md` | 提供 Skill + MCP 的详细调用矩阵 |
| `PROJECT_RULES.md` | 提供编码规范和包结构规范 |
| `ARCHITECTURE.md` | 提供架构约束和数据库/API 定义 |
| `TASK_ROADMAP.md` | 提供 Task 列表和进度追踪 |
| `DEVELOPMENT_PLAN.md` | 提供开发计划和排期 |
| `PRODUCT_DESIGN.md` | 提供产品设计约束 |
| `DESIGN_SYSTEM.md` | 提供设计系统约束 |
| `UI_UX_SPEC.md` | 提供 UI/UX 约束 |

---

> 本文件定义企业级项目执行模式，所有开发任务默认在此模式下执行。  
> 除非用户明确要求退出执行模式，否则永久保持该模式。  
> 生成时间：2026-06-24
