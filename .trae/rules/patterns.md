<!-- AUTO-GENERATED by sync-ai v1.0.0 -->
<!-- Source: docker/patterns.md | Profiles: docker git java mysql personal redis springboot vue web -->
<!-- DO NOT EDIT -->

# Docker 容器化规范

> Canonical Source — 唯一维护

## Dockerfile

- 使用多阶段构建减小镜像体积
- 基础镜像使用 `alpine` 或 `slim` 版本
- 不在镜像中硬编码凭据
- `.dockerignore` 排除 `node_modules/`、`.git/`、`target/`

## Docker Compose

- 服务按功能分组
- 使用命名卷（named volumes）持久化数据
- 环境变量通过 `.env` 文件注入（`.env` 不提交）

## 部署

- 生产环境使用 `docker-compose.yml` + Nginx 反向代理
- 健康检查（HEALTHCHECK）配置
- 日志输出到 stdout/stderr（不写文件）
