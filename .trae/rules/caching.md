<!-- AUTO-GENERATED by sync-ai v1.0.0 -->
<!-- Source: redis/caching.md | Profiles: docker git java mysql personal redis springboot vue web -->
<!-- DO NOT EDIT -->

# Redis 缓存规范

> Canonical Source — 唯一维护

## Key 设计

- 格式：`{业务模块}:{标识}:{子标识}`
- 示例：`user:123:profile`, `order:456:detail`
- 统一使用冒号分隔
- 设置合理的 TTL

## 缓存策略

- **Cache-Aside**：先查缓存 → 未命中则查 DB → 写入缓存
- 写操作：先更新 DB → 再删除缓存
- 热点数据预加载

## Spring Cache 集成

```java
@Cacheable(value = "users", key = "#id")
public UserVO findById(Long id) { ... }

@CacheEvict(value = "users", key = "#id")
public void update(Long id, UpdateRequest req) { ... }
```

## 降级处理

Redis 不可用时，降级到数据库查询：

```java
try {
    return redisTemplate.opsForValue().get(key);
} catch (Exception e) {
    log.warn("Redis 不可用，降级到数据库查询");
    return queryFromDatabase(key);
}
```

## 禁止行为

- ❌ 缓存无限增长（必须设 TTL）
- ❌ 缓存包含敏感信息不加密
- ❌ Redis 不可用时直接抛异常而不降级
